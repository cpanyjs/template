name: Upgrade CPany

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Upgrade version'
        required: true
        default: 'latest'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          pacakge=`cat ./package.json`
          pacakge="${content//'%'/'%25'}"
          pacakge="${content//$'\n'/'%0A'}"
          pacakge="${content//$'\r'/'%0D'}"
          echo $package
