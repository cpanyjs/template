name: Upgrade CPany

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Branch prefix to find combinable PRs based on'
        required: true
        default: 'latest'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          pacakge="./package.json"
          echo "${{ fromJson(package).dependencies }}"
          # npm i @cpany/cli@${{ github.event.inputs.version }}
