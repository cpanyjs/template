name: Upgrade CPany

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Upgrade version'
        required: true
        default: 'latest'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          pacakge="./package.json"
          echo "${{ fromJson(env.package).dependencies }}"
          # npm i @cpany/cli@${{ github.event.inputs.version }}
